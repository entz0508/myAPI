// config
var server_info = 'live';	// 'dev', 'test', 'live' ...
var dev_env = 'production';

module.exports = {

    //--------------------------------------------------------
    //-- config
    server_info : server_info,
    dev_env : dev_env
    //--------------------------------------------------------
};